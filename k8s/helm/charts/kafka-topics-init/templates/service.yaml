apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafka-topics-init.fullname" . }}
  labels:
    {{- include "kafka-topics-init.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kafka-topics-init.selectorLabels" . | nindent 4 }}
