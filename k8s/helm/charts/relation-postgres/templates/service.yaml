apiVersion: v1
kind: Service
metadata:
  name: {{ include "relation-postgres.fullname" . }}
  labels:
    {{- include "relation-postgres.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "relation-postgres.selectorLabels" . | nindent 4 }}
