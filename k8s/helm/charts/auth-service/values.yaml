replicaCount: 2

nameOverride: ""
fullnameOverride: auth-service

image:
  repository: pinstack/auth-service
  tag: "dev"
  pullPolicy: IfNotPresent

imagePullSecrets: []

serviceAccount:
  create: false
  name: ""

service:
  type: ClusterIP
  port: 50052

env:
  ENV: dev

db:
  host: auth-db
  port: "5432"
  name: authservice
  user: postgres
  passwordSecret:
    name: auth-db-secret
    key: password

migrator:
  enabled: true
  image:
    repository: pinstack/auth-migrator
    tag: "dev"
    pullPolicy: IfNotPresent
  command:
    - ./migrator
    - -command
    - up
  backoffLimit: 3
  activeDeadlineSeconds: 180
  ttlSecondsAfterFinished: 300
  volumeMounts:
    - name: auth-config
      mountPath: /app/config
      readOnly: true
    - name: auth-migrations
      mountPath: /app/migrations
      readOnly: true
  volumes:
    - name: auth-config
      configMap:
        name: auth-service-config
    - name: auth-migrations
      configMap:
        name: auth-service-migrations

podAnnotations: {}
podLabels: {}

podSecurityContext: {}
securityContext: {}

resources: {}

livenessProbe:
  initialDelaySeconds: 20
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 6

readinessProbe:
  initialDelaySeconds: 5
  periodSeconds: 5
  timeoutSeconds: 3
  failureThreshold: 6

volumes:
  - name: auth-config
    configMap:
      name: auth-service-config
volumeMounts:
  - name: auth-config
    mountPath: /app/config
    readOnly: true

nodeSelector: {}
tolerations: []

affinity: {}
