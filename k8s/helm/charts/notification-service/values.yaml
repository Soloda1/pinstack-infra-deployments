replicaCount: 2

nameOverride: ""
fullnameOverride: notification-service

image:
  repository: pinstack/notification-service
  tag: "dev"
  pullPolicy: IfNotPresent

imagePullSecrets: []

serviceAccount:
  create: false
  name: ""

service:
  type: ClusterIP
  port: 50055

env:
  ENV: dev

db:
  host: notification-db
  port: "5432"
  name: notificationservice
  user: postgres
  passwordSecret:
    name: notification-db-secret
    key: password

migrator:
  enabled: true
  image:
    repository: pinstack/notification-migrator
    tag: "dev"
    pullPolicy: IfNotPresent
  command:
    - ./migrator
    - -command
    - up
  backoffLimit: 3
  activeDeadlineSeconds: 180
  ttlSecondsAfterFinished: 300
  volumeMounts:
    - name: notification-config
      mountPath: /app/config
      readOnly: true
    - name: notification-migrations
      mountPath: /app/migrations
      readOnly: true
  volumes:
    - name: notification-config
      configMap:
        name: notification-service-config
    - name: notification-migrations
      configMap:
        name: notification-service-migrations

podAnnotations: {}
podLabels: {}

podSecurityContext: {}
securityContext: {}

resources: {}

livenessProbe:
  initialDelaySeconds: 20
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 6

readinessProbe:
  initialDelaySeconds: 5
  periodSeconds: 5
  timeoutSeconds: 3
  failureThreshold: 6

volumes:
  - name: notification-config
    configMap:
      name: notification-service-config
volumeMounts:
  - name: notification-config
    mountPath: /app/config
    readOnly: true

nodeSelector: {}
tolerations: []

affinity: {}
